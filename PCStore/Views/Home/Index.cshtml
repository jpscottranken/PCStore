@model HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="row text-center">
    <h1 class="mb-3">Welcome to PC Panda!</h1>
    <div class="col-md-3">
        <img class="img-fluid" src="~/images/PC_Panda.png" />
    </div>
    <div class="col-md-9">
        <h2 class="text-danger">On Sale Now!</h2>
        <partial name="_Carousel" />
@*         <div class="carousel-slide">
            <div class="carousel-inner">
                <div class="carousel-item-active">
                    <img src="~/images/PC_Panda.png" />
                </div>
                @foreach(var product in Model.ProductsOnSale)
                {
                    <div class="carousel-item">
                        <img src="@product.ProductImageURL" />
                    </div>
                }
            </div>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div> *@
    </div>
</div>
<div class="row text-center">
        <a class="btn btn-success w-25" asp-area="" asp-controller="Product" asp-action="List">View All Products</a>
</div>
